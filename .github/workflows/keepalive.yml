name: Keep Railway Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Railway
        run: curl -I https://n8n-production-b2cf.up.railway.app/
